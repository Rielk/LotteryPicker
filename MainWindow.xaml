<Window x:Class="LotteryPicker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LotteryPicker"
        mc:Ignorable="d"
        Title="Lottery Number Picker"
        Height="Auto" Width="250"
        SizeToContent="Height"
        ResizeMode="NoResize">
    <Window.Resources>
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </Window.Resources>
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <StackPanel>
        <Grid Height="100">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" TextWrapping="NoWrap" Text="{Binding Numbers[0], TargetNullValue=-}" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,10,10,10"/>
            <TextBlock Grid.Row="0" Grid.Column="1" TextWrapping="NoWrap" Text="{Binding Numbers[1], TargetNullValue=-}" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,10,10,10"/>
            <TextBlock Grid.Row="0" Grid.Column="2" TextWrapping="NoWrap" Text="{Binding Numbers[2], TargetNullValue=-}" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,10,10,10"/>
            <TextBlock Grid.Row="0" Grid.Column="3" TextWrapping="NoWrap" Text="{Binding Numbers[3], TargetNullValue=-}" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,10,10,10"/>
            <TextBlock Grid.Row="0" Grid.Column="4" TextWrapping="NoWrap" Text="{Binding Numbers[4], TargetNullValue=-}" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,10,10,10"/>
            <TextBlock Grid.Row="1" Grid.Column="2" TextWrapping="NoWrap" Text="{Binding BonusNumber, TargetNullValue=-}" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,10,10,10"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" TextWrapping="NoWrap" Text="Bonus:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10"/>
        </Grid>
        <Button Content="Roll" Margin="10,10,10,10" Click="Button_Click" IsEnabled="{Binding IsRolling, Converter={StaticResource InverseBooleanConverter}}"/>

    </StackPanel>
</Window>
